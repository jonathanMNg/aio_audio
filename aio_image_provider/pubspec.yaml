name: aio_image_provider
description: "A Flutter package that provide utility for image, image search, and image picker"
version: 0.0.1
homepage:

environment:
  sdk: ^3.7.2
  flutter: ">=1.17.0"
dependencies:
  flutter:
    sdk: flutter
  file_picker: ^10.1.9
  http: ^1.4.0
  path_provider: ^2.1.5
  flutter_google_image:
    git:
      url: https://github.com/jonathanMNg/flutter_google_image.git
  crypto: ^3.0.0

dev_dependencies:
  flutter_lints: ^5.0.0

